# Agenda App - Fullstack (Frontend + Backend)

Repositorio generated by ChatGPT. Frontend (React) + Backend (Node/Express) + MongoDB (Mongoose).

## Estructura
- backend/  -> Node.js + Express API
- frontend/ -> React app (Create React App compatible)

## Deploy
- Backend: Render / Railway (set MONGO_URI and FRONTEND_URL env vars)
- Frontend: Netlify (set REACT_APP_API_URL env var to backend URL + '/api')
